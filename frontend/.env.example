# Public URL of the FastAPI backend (no trailing slash), used by production builds.
# Leave UNSET in development — Vite proxies /api to the local backend instead.
# Example: VITE_API_URL=https://api.your-domain.com
VITE_API_URL=
