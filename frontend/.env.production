# Production backend (FastAPI on the Hostinger VPS, fronted by Traefik + TLS).
# Used automatically by `npm run build` on Vercel. Override with a Vercel
# environment variable if the backend URL changes.
VITE_API_URL=https://wcd-api.armageddonvivas.cloud
